{
  "name": "cns-dapr-client",
  "version": "1.0.0",
  "description": "Dapr CNS Client",
  "author": "Padi, Inc",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "start:dapr": "dapr run --app-id cns-dapr-client --app-port 3100 --app-protocol http --resources-path ../cns-dapr/components -- npm run start",
    "get": "node get",
    "post": "node post",
    "publish": "node publish",
    "subscribe": "node subscribe",
    "stop": "killall node",
    "get:dapr": "dapr run --app-id cns-dapr-get --resources-path ../cns-dapr/components -- npm run get",
    "post:dapr": "dapr run --app-id cns-dapr-post --resources-path ../cns-dapr/components -- npm run post",
    "publish:dapr": "dapr run --app-id cns-dapr-publish --resources-path ../cns-dapr/components -- npm run publish",
    "subscribe:dapr": "dapr run --app-id cns-dapr-subscribe --app-port 3100 --app-protocol http --resources-path ../cns-dapr/components -- npm run subscribe",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@dapr/dapr": "^3.1.2",
    "object-merge": "^2.5.1",
    "table": "^6.8.1"
  }
}
